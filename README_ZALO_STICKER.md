# Zalo custom sticker host

File `zalo_sticker_jerry.webp` on branch `zalo-sticker-host` is intended to be served directly through `raw.githubusercontent.com` for the custom sticker sender.

This branch is separate from the main project branch.